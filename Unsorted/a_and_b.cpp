#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin>>t;
    while(t--){
	int n;
	cin>>n;
	string s;
	cin>>s;
	//first of all i only need to worry about either a or b
	//the other one will come on its own
	//lets say that iabab want to cluter either a or b
    }
    return 0;
}
