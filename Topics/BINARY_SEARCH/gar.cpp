#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin>>t;
    while(t--){
	vector<int>v(2*1e5);
	int n;
	cin>>n;
	int k;
	cin>>k;
	for(int i=0;i<k;i++){
	    int p;
	    cin>>p;
	    v[p]++;
	}
    }
    return 0;
}

