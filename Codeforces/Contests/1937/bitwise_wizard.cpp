#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;cin>>t;
    while(t--){
	int n;cin>>n;
	//i must get to 0b11111111111
	//i can do a n^2 algo
	//or never makes an interger smaller
	//let me think
	//i will first choose 4 random integers:
	//if it return < what does this mean
    }
    return 0;
}
