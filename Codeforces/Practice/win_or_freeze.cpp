#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int q;
    //the player who can't make a move wins
    //there are two types of number:winning and losing ones
    //a winning number if the number of his divisors is odd

    return 0;
}
