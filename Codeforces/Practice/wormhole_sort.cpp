#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n,m;
    cin>>n>>m;
    vector<int>v(n);
    for(int i=0;i<n;i++){
	cin>>v[i];
    }
    // the conplexity of dfs is O(n+m)
    // so i can do bs for the min value 
    //

    return 0;
}
