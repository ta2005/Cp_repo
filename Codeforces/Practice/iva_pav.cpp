#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin>>t;
    while(t--){
	int n;
	cin>>n;
	vector<int>v(n);
	//the think about a&b is that it is alwyas <=min(a,b)
	//so the sum can only go down
	for (int i=0; i<n; i++) {
	    //bs??
	    cin>>v[i];
	}
    }

    return 0;
}
